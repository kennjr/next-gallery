
export default function Home() {
  // const response = fetch(`${RANDOM_PHOTO_ENDPOINT}?client_id=${process.env.UNSPLASH_KEY}`)
  // const image: PhotoData = response.json()
  return (
    <main className="flex h-screen flex-col items-center justify-between bg-slate-50 mx-auto py-24">
      {/* <Photo photoData={image}></Photo> */}
    </main>
  );
}
